{
  "name": "dream-site-v3-init",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"pnpm --filter back dev\" \"pnpm --filter front dev\"",
    "build": "pnpm --filter front build && pnpm --filter back build",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:cms": "playwright test tests/cms",
    "test:e2e:pages": "playwright test tests/pages",
    "test:e2e:ci": "playwright install --with-deps && playwright test --reporter=line"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "concurrently": "^8.0.0",
    "typescript": "^5.3.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/creativeeyestudio/dream-site-v3-init"
  },
  "private": true
}
